# Third-party notices

Ứng dụng sử dụng các thư viện mã nguồn mở được khai báo trong `package.json`/`package-lock.json`. Các gói chính gồm React, Next.js/Vinext, Tailwind CSS, shadcn/Radix UI, Recharts, Lucide, Drizzle ORM và các runtime xuất tệp sau:

- SheetJS Community Edition (`xlsx.full.min.js`), giấy phép Apache-2.0.
- docx (`docx.iife.js`), giấy phép MIT.
- pdfmake 0.2.12 và Roboto VFS, giấy phép MIT/Apache-2.0 tương ứng.

Bản quyền và điều khoản chi tiết thuộc về từng dự án upstream. Các tệp runtime được đóng gói cục bộ để chức năng nhập/xuất không phụ thuộc CDN bên thứ ba.

Logo UED là dấu hiệu nhận diện của Trường Đại học Sư phạm – Đại học Đà Nẵng, lấy từ website chính thức của trường và chỉ dùng cho ứng dụng nội bộ theo yêu cầu nghiệp vụ; không phải tài sản mã nguồn mở của dự án.
